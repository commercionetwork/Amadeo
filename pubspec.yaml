name: amadeo_flutter
description: Amadeo is a tarter/cookbook Flutter projec for Commercio.network.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  auto_route: ^0.5.0
  bloc: ^4.0.0
  commerciosdk: ^2.1.0
  commercio_ui: ^0.0.1
  flutter_bloc: ^4.0.0
  cupertino_icons: ^0.1.3
  log_4_dart_2: ^0.4.0
  provider: ^4.1.2
  sacco: ^0.1.4
  qr_flutter: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  auto_route_generator: ^0.5.0
  build_runner: ^1.10.0
  lint: ^1.2.0
  mockito: ^4.1.1
  test_coverage: ^0.4.1

flutter:
  uses-material-design: true
